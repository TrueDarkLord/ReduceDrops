name: ReduceDrops
version: '${version}'
main: me.truedarklord.reduceDrops.ReduceDrops
api-version: '1.21'
prefix: Reduce Drops
authors: [ TrueDarkLord ]
description: Automatically pickup items instead of them dropping onto the ground.
commands:
  reload:
    usage: /<command>
    permission: reduceddrops.reload
    description: Reloads the plugin.